# react-native-signature-canvas déclare des peer dependencies React antérieures
# à React 19 : sans ce réglage, `npm ci` échoue sur ERESOLVE et le build EAS
# s'arrête dès la phase « Install dependencies ».
legacy-peer-deps=true
